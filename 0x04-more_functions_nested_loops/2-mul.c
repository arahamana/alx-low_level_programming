#include "main.h"
#include <stdio.h>

/**
 * mul - multiples two integers
 *
 * @a: integer to be multiplied to b
 * @b: integer to be multiplied to a
 * Return: a * b
 */
int mul(int a, int b)
{
	return (a * b);
}
